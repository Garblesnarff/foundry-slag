export interface Job { id: string; status: 'queued'|'processing'|'completed'|'failed'; mode: 'single'|'batch'; model: string; total_images: number; completed_images: number; failed_images: number; processing_time_seconds: number | null; created_at: string; started_at: string | null; completed_at: string | null; error_message: string | null; }
export interface RefinementSettings { edge_feather: number; edge_shift: number; bg_type: 'transparent'|'color'|'image'; bg_color: string; bg_image_path: string | null; shadow_enabled: boolean; shadow_opacity: number; shadow_offset_x: number; shadow_offset_y: number; shadow_blur: number; }
export interface ProcessedImage { id: string; job_id: string; status: 'queued'|'processing'|'completed'|'failed'; original_filename: string; input_width: number; input_height: number; input_format: string; input_file_size_bytes: number; output_width: number | null; output_height: number | null; output_file_size_bytes: number | null; processing_time_seconds: number | null; error_message: string | null; original_url: string; result_url: string; thumbnail_url: string; refinements: RefinementSettings; created_at: string; }
export interface HistoryStats { session: { images_processed: number; total_processing_seconds: number; avg_time_per_image: number; }; lifetime: { images_processed: number; total_processing_seconds: number; avg_time_per_image: number; }; }
export interface Settings { model: string; output_format: string; alpha_matting: boolean; output_directory: string; auto_cleanup_days: number; default_bg_type: string; default_bg_color: string; thumbnail_enabled: boolean; }
export interface HealthStatus { status: 'loading'|'ready'|'processing'|'error'; model: string; model_loaded: boolean; processing: boolean; queue_length: number; }
export const PRESET_BG_COLORS = ['#FFFFFF', '#000000', '#F5F5F5', '#E8E8E8', '#FF5733', '#3498DB', '#2ECC71', 'transparent'];
