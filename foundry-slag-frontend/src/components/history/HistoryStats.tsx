export default function HistoryStats({ stats }: { stats: any }) { return <div className="grid grid-cols-4 gap-3">{['IMAGES SLAGGED','TOTAL TIME','AVG PER IMAGE','TOTAL SAVED'].map((label,i)=><div key={label} className="card"><p className="mono text-xs">{label}</p><p className="text-xl">{i===0?stats?.lifetime?.images_processed ?? 0:i===1?stats?.lifetime?.total_processing_seconds ?? 0:i===2?stats?.lifetime?.avg_time_per_image ?? 0:'—'}</p></div>)}</div>; }
